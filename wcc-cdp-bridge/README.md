# WCC CDP Bridge Server

**Chrome DevTools Protocol Bridge** для подключения реальных кошельков (MetaMask, Phantom) к WCC Admin Panel.

## 🎯 Что это?

CDP Bridge Server — это локальный Node.js микросервис, который:
1. Запускает Chrome с установленными расширениями кошельков
2. Подключается к Chrome через Chrome DevTools Protocol (CDP)
3. Читает DOM, стили, делает скриншоты
4. Передает данные в WCC Admin через WebSocket
5. Позволяет применять темы в реальном времени

## 📦 Установка

```bash
cd wcc-cdp-bridge
npm install
```

## 🚀 Запуск

```bash
npm start
```

Сервер запустится на `http://localhost:4000`

## 🔧 Требования

- **Node.js** 16+
- **Chrome/Chromium** установлен в системе
- **MetaMask** или **Phantom** расширения установлены в Chrome

## 🎨 Использование с WCC

1. Запустите CDP Bridge Server:
   ```bash
   npm start
   ```

2. В WCC Admin откройте Wallet Preview

3. Кликните на dropdown "WCC" → выберите "MetaMask" или "Phantom"

4. В открывшемся диалоге:
   - Нажмите "Start Chrome" → запустится Chrome с кошельком
   - Нажмите "Scan DOM" → получите screenshot и DOM
   - Настройте цвета в Theme Controls
   - Нажмите "Apply Theme" → увидите изменения в реальном времени

## 🏗️ Архитектура

```
WCC Admin (React)
    ↓ WebSocket (Socket.io)
CDP Bridge Server (Node.js)
    ↓ Chrome DevTools Protocol
Chrome + MetaMask/Phantom Extension
```

## 📡 WebSocket API

### События от клиента:

**`startChrome`**
```javascript
socket.emit('startChrome', { walletType: 'metamask' | 'phantom' })
```

**`scanDom`**
```javascript
socket.emit('scanDom')
```

**`applyTheme`**
```javascript
socket.emit('applyTheme', { 
  cssRules: ['body { background: #000; }', ...] 
})
```

### События к клиенту:

**`log`** - текстовые логи
```javascript
socket.on('log', (message: string) => { ... })
```

**`domSnapshot`** - HTML содержимое
```javascript
socket.on('domSnapshot', ({ html: string }) => { ... })
```

**`screenshot`** - скриншот страницы
```javascript
socket.on('screenshot', ({ dataUrl: string }) => { ... })
```

**`themeApplied`** - результат применения темы
```javascript
socket.on('themeApplied', ({ ok: boolean, error?: string }) => { ... })
```

## 🔐 Безопасность

- ✅ Работает только локально (localhost)
- ✅ Не передает приватные ключи
- ✅ Не инициирует транзакции
- ✅ Только визуальный слой (DOM + styles)

## 🛠️ Конфигурация

Переменные окружения (опционально):

```bash
METAMASK_ID=nkbihfbeogaeaoehlefnkodbefgpgknn  # ID расширения MetaMask
PHANTOM_ID=bfnaelmomeimhlpmgjnjophhpkkoljpa   # ID расширения Phantom
```

## 📝 Логи

Сервер пишет логи в консоль:
- 🟢 Подключение клиента
- 🔵 События WebSocket
- ❌ Ошибки

## 🐛 Troubleshooting

**"Cannot connect to CDP Bridge"**
- Убедитесь, что сервер запущен (`npm start`)
- Проверьте, что порт 4000 свободен

**"No page open"**
- Сначала нажмите "Start Chrome"
- Дождитесь сообщения "Ready to scan DOM"

**"Extension not found"**
- Установите MetaMask или Phantom в Chrome
- Убедитесь, что ID расширения правильный

## 📚 Технологии

- **Express** - HTTP server
- **Socket.io** - WebSocket коммуникация
- **Puppeteer** - Chrome automation
- **Chrome DevTools Protocol** - доступ к DOM/styles

## 🎓 Для разработчиков

### Добавление нового кошелька

1. Добавьте ID расширения в конфигурацию
2. Обновите `startChrome` handler для нового типа
3. Протестируйте с реальным расширением

### Расширение API

Вы можете добавить новые события:
- Клик по элементам
- Навигация между экранами
- Экспорт стилей конкретных элементов

---

**Made with ❤️ by WCC Team**
