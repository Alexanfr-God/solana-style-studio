
[install]
timeout = "30m"
cache = true
registry = "https://registry.npmjs.org/"
scopes = {}
peer = true

[install.scopes]
"*" = { production = false }

# Don't trust any packages to run postinstall scripts
[install.trustedDependencies]
# Empty - no packages trusted to run scripts
