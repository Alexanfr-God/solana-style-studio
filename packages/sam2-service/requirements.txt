# SAM2 Service Dependencies
# Python 3.10+ required

torch>=2.5.1
torchvision>=0.20.1
numpy>=1.24.0
Pillow>=9.0.0

# SAM2 dependencies (from facebookresearch/sam2)
hydra-core>=1.3.0
iopath>=0.1.10
tqdm>=4.64.0

# Note: SAM2 itself is installed from vendor/sam2:
#   cd vendor/sam2 && pip install -e .
